package day058;

public class ExceptionContinues {

    /*Exception: Is an event , which occurs during the execution of a program

    * COMPILE TIME:
    COMPILE ERRORS:Because of programmer's mistake
    regarding syntax error, data types errors happens
    DURING RUNTIME:
    ERROR:Because of environment issues happens, we do not handle them


    CHECKED EXCEPTIONS:Programmer must handle
     otherwise code will fail to compile

    UNCHECKED EXCEPTIONS:are exceptions that happen during runtime
    due to programmer's mistake.
    It is optional to handle them
    Mostly programmer should do his/her best to fix code and
    avoid Unchecked Exceptions



    StackOverflowError happens when stack memory is full
    OutOfMemoryError  happens when heap memory is full

    */
}
